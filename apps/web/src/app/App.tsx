import tw, {styled} from 'twin.macro';

const StyledApp = styled.div`
  ${tw`bg-sky-400 h-screen w-screen`}
`;

export function App() {
  return (
    <StyledApp>

    </StyledApp>
  );
}

export default App;
